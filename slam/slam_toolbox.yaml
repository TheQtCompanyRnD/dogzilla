# Copyright (C) 2026 The Qt Company Ltd.
# SPDX-License-Identifier: LicenseRef-Qt-Commercial OR BSD-3-Clause
#
# slam_toolbox (async online mapping) tuned for the Dogzilla S2 + Oradar MS200.
# Frames follow the TF tree: map -> odom (slam_toolbox) -> base_link (rf2o) ->
# laser_frame (dogzillad static TF, from the URDF laser_Joint origin).

slam_toolbox:
  ros__parameters:
    # --- Frames & topics -----------------------------------------------------
    odom_frame: odom
    map_frame: map
    base_frame: base_link
    scan_topic: /dogzilla/sensor_msgs/msg/LaserScan
    use_sim_time: false

    # --- Mode ----------------------------------------------------------------
    mode: mapping            # 'mapping' to build, 'localization' to reuse a map
    # map_file_name: /home/rutledge/maps/lab   # uncomment + set for localization
    # map_start_pose: [0.0, 0.0, 0.0]

    # --- Solver --------------------------------------------------------------
    solver_plugin: solver_plugins::CeresSolver
    ceres_linear_solver: SPARSE_NORMAL_CHOLESKY
    ceres_preconditioner: SCHUR_JACOBI
    ceres_trust_strategy: LEVENBERG_MARQUARDT
    ceres_dogleg_type: TRADITIONAL_DOGLEG
    ceres_loss_function: None

    # --- Rates ---------------------------------------------------------------
    # The Pi 5 handles these comfortably for a single 360-point scanner.
    throttle_scans: 1
    transform_publish_period: 0.02   # 50 Hz map->odom
    map_update_interval: 2.0
    resolution: 0.05
    max_laser_range: 12.0            # MS200 realistic ceiling (lidar.cpp caps at 20)
    minimum_time_interval: 0.2
    transform_timeout: 0.3
    tf_buffer_duration: 10.0
    stack_size_to_use: 40000000

    # --- Scan matching -------------------------------------------------------
    # A walking gait rocks the lidar, so be a little more permissive about motion
    # before forcing a new node, and keep scan matching on to correct rf2o drift.
    use_scan_matching: true
    use_scan_barycenter: true
    minimum_travel_distance: 0.2
    minimum_travel_heading: 0.2
    scan_buffer_size: 10
    scan_buffer_maximum_scan_distance: 10.0
    link_match_minimum_response_fine: 0.1
    link_scan_maximum_distance: 1.5
    loop_search_maximum_distance: 3.0
    do_loop_closing: true
    loop_match_minimum_chain_size: 10
    loop_match_maximum_variance_coarse: 3.0
    loop_match_minimum_response_coarse: 0.35
    loop_match_minimum_response_fine: 0.45

    # Correlation search windows
    correlation_search_space_dimension: 0.5
    correlation_search_space_resolution: 0.01
    correlation_search_space_smear_deviation: 0.1
    loop_search_space_dimension: 8.0
    loop_search_space_resolution: 0.05
    loop_search_space_smear_deviation: 0.03

    # Penalties
    distance_variance_penalty: 0.5
    angle_variance_penalty: 1.0
    fine_search_angle_offset: 0.00349
    coarse_search_angle_offset: 0.349
    coarse_angle_resolution: 0.0349
    minimum_angle_penalty: 0.9
    minimum_distance_penalty: 0.5
    use_response_expansion: true
